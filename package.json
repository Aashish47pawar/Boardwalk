{
  "name": "collaborative-task-manager",
  "version": "1.0.0",
  "description": "Full-stack collaborative task manager — MongoDB, Express, React, Node.js, Redux Toolkit, JWT, Socket.io.",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
